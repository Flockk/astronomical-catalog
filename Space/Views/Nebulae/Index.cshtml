@model IEnumerable<Space.Models.Nebulae>

@{
    ViewData["Title"] = "Туманности";
}

<h1>Туманности</h1>

<p>
    <a asp-action="Create">Добавить</a>
</p>

<p>
    <span class="input-group-addon"><i class="glyphicon glyphicon-search"></i></span>
    <input type="text" id="Search" placeholder="Поиск" class="form-control" autocomplete="off" />
</p>

<table class="table table-striped table-bordered table-sm" cellspacing="0" width="100%">
    <thead>
        <tr>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["NebulaNameSortParm"]">@Html.DisplayNameFor(model => model.NebulaName)</a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["NebulaTypeSortParm"]">@Html.DisplayNameFor(model => model.NebulaType)</a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["NebulaRightAscensionSortParm"]">@Html.DisplayNameFor(model => model.NebulaRightAscension)</a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["NebulaDeclinationSortParm"]">@Html.DisplayNameFor(model => model.NebulaDeclination)</a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["NebulaDistanceSortParm"]">@Html.DisplayNameFor(model => model.NebulaDistance)</a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["ConsSortParm"]">@Html.DisplayNameFor(model => model.Cons)</a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["GlxSortParm"]">@Html.DisplayNameFor(model => model.Glx)</a>
            </th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr class="Search">
            <td>
                @Html.DisplayFor(modelItem => item.NebulaName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NebulaType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NebulaRightAscension)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NebulaDeclination)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NebulaDistance)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cons.ConsName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Glx.GlxName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NebulaId">Изменить</a> |
                <a asp-action="Details" asp-route-id="@item.NebulaId">Детали</a> |
                <a asp-action="Delete" asp-route-id="@item.NebulaId">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>

<script src="~/lib/jquery/dist/jquery.min.js"></script>
<script>
    $(document).ready(function () {
        function Contains(text_one, text_two) {
            if (text_one.indexOf(text_two) != -1) {
                return true
            }
        }
        $("#Search").keyup(function () {
            var searchText = $("#Search").val().toLowerCase();
            $(".Search").each(function () {
                if (!Contains($(this).text().toLowerCase(), searchText)) {
                    $(this).hide();
                }
                else {
                    $(this).show();
                }
            });
        });
    });
</script>