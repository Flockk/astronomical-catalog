@model Space.Models.PlanetarySystems

@{
	var imgText = "";
	imgText = Model.PlanetsystemName.Trim();
}

<img src="@Model.PlanetsystemImage" width="400" height="400" class="img-fluid" alt="@imgText" title="@imgText" />