@model Space.Models.GalaxyClusters

@{
	var imgText = "";
	imgText = Model.GlxclusterName.Trim();
}

<img src="@Model.GlxclusterImage" width="400" height="400" class="img-fluid"  alt="@imgText" title="@imgText"/>
