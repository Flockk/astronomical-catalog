@model IEnumerable<Space.Models.BlackHoles>

@{
    ViewData["Title"] = "Чёрные дыры";
}

<h1>Чёрные дыры</h1>

<p>
    <a asp-action="Create">Добавить</a>
</p>

<p>
    <span class="input-group-addon"><i class="glyphicon glyphicon-search"></i></span>
    <input type="text" id="Search" placeholder="Поиск" class="form-control" autocomplete="off" />
</p>

<table class="table table-striped table-bordered table-sm" cellspacing="0" width="100%">
    <thead>
        <tr>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["BlackholeNameSortParm"]">@Html.DisplayNameFor(model => model.BlackholeName)</a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["BlackholeTypeSortParm"]">@Html.DisplayNameFor(model => model.BlackholeType)</a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["BlackholeRightAscensionSortParm"]">@Html.DisplayNameFor(model => model.BlackholeRightAscension)</a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["BlackholeDeclinationSortParm"]">@Html.DisplayNameFor(model => model.BlackholeDeclination)</a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["BlackholeDistanceSortParm"]">@Html.DisplayNameFor(model => model.BlackholeDistance)</a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["ConsSortParm"]">@Html.DisplayNameFor(model => model.Cons)</a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewData["GlxSortParm"]">@Html.DisplayNameFor(model => model.Glx)</a>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr class="Search">
            <td>
                @Html.DisplayFor(modelItem => item.BlackholeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BlackholeType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BlackholeRightAscension)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BlackholeDeclination)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BlackholeDistance)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cons.ConsName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Glx.GlxName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BlackHoleId">Изменить</a> |
                <a asp-action="Details" asp-route-id="@item.BlackHoleId">Детали</a> |
                <a asp-action="Delete" asp-route-id="@item.BlackHoleId">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>

<script src="~/lib/jquery/dist/jquery.min.js"></script>
<script>
    $(document).ready(function () {
        function Contains(text_one, text_two) {
            if (text_one.indexOf(text_two) != -1) {
                return true
            }
        }
        $("#Search").keyup(function () {
            var searchText = $("#Search").val().toLowerCase();
            $(".Search").each(function () {
                if (!Contains($(this).text().toLowerCase(), searchText)) {
                    $(this).hide();
                }
                else {
                    $(this).show();
                }
            });
        });
    });
</script>